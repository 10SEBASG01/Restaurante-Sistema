from django.apps import AppConfig

class MenuConfig(AppConfig):
    """
    Configuración de la aplicación de Menú.
    Define el path jerárquico dentro del contenedor de aplicaciones (apps).
    """
    name = 'apps.menu'